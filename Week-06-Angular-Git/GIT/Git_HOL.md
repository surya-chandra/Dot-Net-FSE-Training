# Git Hands-On Exercises (HOL 1 to 5)

## HOL 1: Environment Setup and Initial Repository
- Configured user name and user email.
- Integrated notepad++ as the default editor.
- Created local repository `GitDemo` and added `welcome.txt`.

## HOL 2: Git Ignore
- Configured `.gitignore` to exclude all `.log` files and the `/log` folder from being tracked.

## HOL 3: Branching and Merging
- Created branch `GitNewBranch`.
- Added files, committed changes, merged back into `master`, and deleted the branch.

## HOL 4: Conflict Resolution
- Created branch `GitWork` with conflicting changes in `hello.xml`.
- Performed a 3-way merge onto `master` and manually resolved conflicts.

## HOL 5: Remote Repository Synchronization
- Verified repository status, pulled remote master branch, and prepared changes for remote push.
